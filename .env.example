# Database Configuration for FastAPI/SQLModel
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cartographie

# Optional: Set to 'true' for database query logging
DB_ECHO=false
